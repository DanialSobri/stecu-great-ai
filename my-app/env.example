# Environment variables for Amplify frontend-only deployment
# Add these to your Amplify console under App settings > Environment variables

# Production environment
NODE_ENV=production
NEXT_PUBLIC_APP_ENV=production

# Optional: Add your own environment variables here
# Note: Only NEXT_PUBLIC_* variables are available in the browser
# NEXT_PUBLIC_API_URL=your-api-url
# NEXT_PUBLIC_ANALYTICS_ID=your-analytics-id
